# Project Context (Always On)

- Always read `AGENTS.md` at the start of a new session
- Reference `docs/backlog.md` to understand current project state
- Use `@/path/to/file` mentions when referencing specific files
- Respect the active stack set in `.env` — do not mix stack files
- All Docker services must have health checks defined
